import { LocalePrefix, Pathnames } from "next-intl/navigation";

export const locales = ["en", "ar", "fr", "es", "de"];

export const pathnames = {
  "/": "/",
  "/pathnames": "/pathnames",
};
export const localePrefix = "always"; // أو 'as-needed'
